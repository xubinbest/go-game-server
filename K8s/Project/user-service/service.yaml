apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: game-server
spec:
  selector:
    app: user-service
  ports:
    - name: grpc
      port: 50052
      targetPort: 50052
  type: ClusterIP
