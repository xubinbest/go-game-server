apiVersion: v1
kind: Service
metadata:
  name: test-service
  namespace: game-server
spec:
  type: ClusterIP
  selector:
    app: test-service
  ports:
  - name: grpc
    port: 50053
    targetPort: 50053